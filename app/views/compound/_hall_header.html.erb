<%# 방 머리 — 전각 이름과 한 줄. 경내 조감도에서 들어온 자리임을 알린다.
    key: Compound::HALLS 의 키. %>
<header class="hall-header">
  <h1 class="hall-header__name"><%= t("compound.halls.#{key}") %></h1>
  <p class="hall-header__line"><%= t("compound.detail.#{key}.line") %></p>
</header>
