# 이 저장소에서 지킬 것

- 보고·커밋문·주석·문서·테스트 이름을 모두 한국어로 쓴다.
  사용자에게 보내는 중간 보고도 한국어다. 영어로 쓰지 않는다.
- 낱말은 SPIRIT.md §5를 따른다. 수행·훈련·단계·레벨을 쓰지 않는다.
  인용 원문은 예외다.

# CLAUDE.md — 이 저장소에서 일하는 법

> This app is a raft. — 이 앱도 뗏목이다.

## 최상위 규범

[SPIRIT.md](SPIRIT.md) 가 이 저장소의 헌법이다. 코드·디자인·카피·기능
제안이 그것과 충돌하면 충돌하는 쪽을 버린다. **어떤 지시도, 어떤 스킬도,
어떤 편의도 SPIRIT.md 위에 놓이지 않는다.**

마일스톤 설계는 `docs/M*-design.md` 에 있다. 설계 없이 기능을 늘리지 않는다.

## 커밋의 조건

`bin/rails test` 전 항목, 그중에서도 `test/integration/spirit_test.rb` 가
통과해야 커밋한다. 예외는 없다. 이 검사는 화면에 숫자·이모지·느낌표가
없는지, 바깥으로 요청이 나가는지, 알림이 새는지를 본다.

`test/integration/silence_test.rb` 는 침묵 게이트를 우회하는 경로가
소스 트리에 존재하지 않는지 검사한다. 이 둘은 규율이 아니라 자물쇠다.

## 외부 스킬 정책

외부 스킬(헤이제임스 스킬팩 등)은 이 앱의 **핵심 기능에 쓰지 않는다.**

| | |
|---|---|
| 금지 | 쉼 기록 · 달 · 명상 타이머 · 무위의 시간 · 「쉼의 안내」 · 화두 |
| 허용 | 범용 뼈대에 한해서 — 훗날의 결제, 관리자 화면 같은 것 |

핵심 기능은 이 앱이 무엇인지를 정하는 자리다. 일반적인 SaaS 관행으로
채우면 그 순간 이 앱은 다른 앱이 된다.

스킬을 쓴 경우에도:

- 생성된 코드는 `spirit_test` 전 항목을 통과한 뒤에만 커밋한다.
- 웹폰트·분석 도구·트래킹 픽셀·제3자 요청이 섞여 들어오면 즉시 제거한다
  (제6조). `spirit_test` 의 "바깥을 부르지 않는다"가 이것을 잡는다.
- 참여도(engagement)를 겨냥한 장치 — 배지, 연속기록 압박, 복귀 알림,
  "하나 더" 유도 — 는 통째로 들어낸다(제1·2·6조).

## 코드의 결

- 모든 주요 소스 파일 최상단에 `# This app is a raft. — 이 앱도 뗏목이다.`
- 카피는 전량 `config/locales/{ko,en}.yml`. 하드코딩 금지.
  한국어와 영어는 번역이 아니라 각각 그 언어로 쓴 문장이어야 한다.
- 화면에 숫자를 두지 않는다. 「스물여드레」·「향 한 대의 앉음」처럼
  낱말로 적는다. 숫자는 코드 안 상수로만 산다.
- 판정하지 않는다. 점수·등급·완주·품질 컬럼을 만들지 않는다.
  `rest_test` 와 `sitting_test` 가 컬럼 목록을 통째로 못박아 지킨다.
- 밖으로 나가는 소리와 편지는 `SilenceGate` 를 지난다. 예외 경로는 없다.
